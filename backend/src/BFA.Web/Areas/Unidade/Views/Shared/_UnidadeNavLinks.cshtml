@{
    var caminhoAtual = Context.Request.Path.Value?.TrimEnd('/') ?? string.Empty;
    var unidadeId = Context.Request.RouteValues["unidadeId"]?.ToString();
    var rotaUnidade = $"/unidade/{unidadeId}";
    var rotaContrato = $"{rotaUnidade}/contrato";
    var rotaProfessores = $"{rotaUnidade}/professores";
    var rotaTurmas = $"{rotaUnidade}/turmas";
    var rotaAlunos = $"{rotaUnidade}/alunos";
    var rotaAulas = $"{rotaUnidade}/aulas";
    var rotaPlanos = $"{rotaUnidade}/planos";
    var rotaMatriculas = $"{rotaUnidade}/matriculas";
    var rotaFinanceiro = $"{rotaUnidade}/cobrancas";
    var rotaRelatorios = $"{rotaUnidade}/relatorios";
    var contratoAtivo = caminhoAtual.StartsWith(
        rotaContrato,
        StringComparison.OrdinalIgnoreCase);
    var professoresAtivo = caminhoAtual.StartsWith(
        rotaProfessores,
        StringComparison.OrdinalIgnoreCase);
    var turmasAtivo = caminhoAtual.StartsWith(
        rotaTurmas,
        StringComparison.OrdinalIgnoreCase);
    var alunosAtivo = caminhoAtual.StartsWith(
        rotaAlunos,
        StringComparison.OrdinalIgnoreCase);
    var aulasAtivo = caminhoAtual.StartsWith(
        rotaAulas,
        StringComparison.OrdinalIgnoreCase);
    var planosAtivo = caminhoAtual.StartsWith(
        rotaPlanos,
        StringComparison.OrdinalIgnoreCase);
    var matriculasAtivo = caminhoAtual.StartsWith(
        rotaMatriculas,
        StringComparison.OrdinalIgnoreCase);
    var financeiroAtivo = caminhoAtual.StartsWith(
        rotaFinanceiro,
        StringComparison.OrdinalIgnoreCase);
    var relatoriosAtivo = caminhoAtual.StartsWith(
        rotaRelatorios,
        StringComparison.OrdinalIgnoreCase);
    var visaoGeralAtiva = !contratoAtivo && !professoresAtivo && !turmasAtivo
        && !alunosAtivo && !aulasAtivo && !planosAtivo && !matriculasAtivo
        && !financeiroAtivo && !relatoriosAtivo
        && caminhoAtual.Equals(rotaUnidade, StringComparison.OrdinalIgnoreCase);
}

<a class="bfa-admin-nav-link @(visaoGeralAtiva ? "is-active" : null)"
   href="@rotaUnidade"
   aria-current="@(visaoGeralAtiva ? "page" : null)">
    <svg viewBox="0 0 24 24" aria-hidden="true" focusable="false">
        <path d="M4 4h6v6H4z"></path>
        <path d="M14 4h6v6h-6z"></path>
        <path d="M4 14h6v6H4z"></path>
        <path d="M14 14h6v6h-6z"></path>
    </svg>
    <span>Visão Geral</span>
</a>

<a class="bfa-admin-nav-link @(professoresAtivo ? "is-active" : null)"
   href="@rotaProfessores"
   aria-current="@(professoresAtivo ? "page" : null)">
    <svg viewBox="0 0 24 24" aria-hidden="true" focusable="false">
        <path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"></path>
        <circle cx="9" cy="7" r="4"></circle>
        <path d="M19 8v6M16 11h6"></path>
    </svg>
    <span>Professores</span>
</a>

<a class="bfa-admin-nav-link @(turmasAtivo ? "is-active" : null)"
   href="@rotaTurmas"
   aria-current="@(turmasAtivo ? "page" : null)">
    <svg viewBox="0 0 24 24" aria-hidden="true" focusable="false">
        <path d="M4 5h16v14H4z"></path>
        <path d="M8 3v4M16 3v4M4 9h16M8 13h3M13 13h3M8 16h3"></path>
    </svg>
    <span>Turmas</span>
</a>

<a class="bfa-admin-nav-link @(alunosAtivo ? "is-active" : null)"
   href="@rotaAlunos"
   aria-current="@(alunosAtivo ? "page" : null)">
    <svg viewBox="0 0 24 24" aria-hidden="true" focusable="false">
        <path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"></path>
        <circle cx="9" cy="7" r="4"></circle>
        <path d="M19 8v6M16 11h6"></path>
    </svg>
    <span>Alunos</span>
</a>

<a class="bfa-admin-nav-link @(aulasAtivo ? "is-active" : null)"
   href="@rotaAulas"
   aria-current="@(aulasAtivo ? "page" : null)">
    <svg viewBox="0 0 24 24" aria-hidden="true" focusable="false">
        <rect x="3" y="4" width="18" height="18" rx="2"></rect>
        <path d="M16 2v4M8 2v4M3 10h18M8 14h.01M12 14h.01M16 14h.01M8 18h.01M12 18h.01"></path>
    </svg>
    <span>Aulas</span>
</a>

<a class="bfa-admin-nav-link @(planosAtivo ? "is-active" : null)"
   href="@rotaPlanos"
   aria-current="@(planosAtivo ? "page" : null)">
    <svg viewBox="0 0 24 24" aria-hidden="true" focusable="false">
        <path d="M4 5h16v14H4z"></path>
        <path d="M8 9h8M8 13h5M8 17h3"></path>
    </svg>
    <span>Planos</span>
</a>

<a class="bfa-admin-nav-link @(matriculasAtivo ? "is-active" : null)"
   href="@rotaMatriculas"
   aria-current="@(matriculasAtivo ? "page" : null)">
    <svg viewBox="0 0 24 24" aria-hidden="true" focusable="false">
        <path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"></path>
        <circle cx="9" cy="7" r="4"></circle>
        <path d="M16 11l2 2 4-4"></path>
    </svg>
    <span>Matrículas</span>
</a>

<a class="bfa-admin-nav-link @(financeiroAtivo ? "is-active" : null)"
   href="@rotaFinanceiro"
   aria-current="@(financeiroAtivo ? "page" : null)">
    <svg viewBox="0 0 24 24" aria-hidden="true" focusable="false">
        <path d="M12 2v20M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"></path>
    </svg>
    <span>Financeiro</span>
</a>

<a class="bfa-admin-nav-link @(relatoriosAtivo ? "is-active" : null)"
   href="@rotaRelatorios"
   aria-current="@(relatoriosAtivo ? "page" : null)">
    <svg viewBox="0 0 24 24" aria-hidden="true" focusable="false">
        <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"></path>
        <path d="M14 2v6h6M16 13H8M16 17H8M10 9H8"></path>
    </svg>
    <span>Relatórios</span>
</a>

<a class="bfa-admin-nav-link @(contratoAtivo ? "is-active" : null)"
   href="@rotaContrato"
   aria-current="@(contratoAtivo ? "page" : null)">
    <svg viewBox="0 0 24 24" aria-hidden="true" focusable="false">
        <path d="M6 3h9l3 3v15H6z"></path>
        <path d="M15 3v4h4M9 11h6M9 15h6"></path>
    </svg>
    <span>Contrato</span>
</a>
